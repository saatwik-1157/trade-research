{
  "$comment": [
    "GENERATED by tools/project_state.py. Do not hand-edit: the previous",
    "hand-written version drifted from the database on six counts at once,",
    "each of which was true when written.",
    "A figure that could not be measured is absent from `measured` and named",
    "in `unmeasured`. It is NEVER written as 0 -- zero is a measurement."
  ],
  "generated_at": "2026-09-18T21:54:37.340236+00:00",
  "generated_by": "tools/project_state.py",
  "measurement_source": "the running deployment: tr-postgres, and the API health endpoint through the nginx proxy if it is up or on its own port if not",
  "trading_mode": "paper",
  "live_trading": false,
  "live_execution_allowed": false,
  "live_blockers": 12,
  "api_reachable": true,
  "api_reached_via": "direct",
  "schema_head": "0027_capital_reservations",
  "measured": {
    "market_bars": 705,
    "market_bar_symbols": 2,
    "datasets": 1,
    "datasets_ready": 1,
    "model_versions": 1,
    "training_runs": 3,
    "strategies": 2,
    "strategy_versions": 3,
    "bots": 8,
    "orders": 289,
    "trades": 252,
    "positions_open": 2,
    "capital_reservations": 0,
    "signals": 124,
    "accounts_paper": 7,
    "accounts_broker": 1
  },
  "unmeasured": [],
  "provenance": {
    "market_bars": "select count(*) from market_bars",
    "market_bar_symbols": "select count(distinct provider_symbol) from market_bars",
    "datasets": "select count(*) from datasets",
    "datasets_ready": "select count(*) from datasets where status = 'READY'",
    "model_versions": "select count(*) from model_versions",
    "training_runs": "select count(*) from training_runs",
    "strategies": "select count(*) from strategies",
    "strategy_versions": "select count(*) from strategy_versions",
    "bots": "select count(*) from bots",
    "orders": "select count(*) from orders",
    "trades": "select count(*) from trades",
    "positions_open": "select count(*) from positions where status = 'open'",
    "capital_reservations": "select count(*) from capital_reservations",
    "signals": "select count(*) from signals",
    "accounts_paper": "select count(*) from paper_accounts",
    "accounts_broker": "select count(*) from broker_accounts"
  },
  "repo": {
    "backend_modules": 332,
    "backend_test_files": 79,
    "frontend_sources": 122,
    "documents": 182,
    "migrations": 27
  },
  "safety": {
    "available": true,
    "total": 31,
    "by_status": {
      "ENFORCED": 29,
      "NOT_APPLICABLE": 2,
      "UNENFORCED": 0
    },
    "certification": "CONDITIONALLY_CERTIFIED",
    "gates_not_passing": [
      "GATE-01",
      "GATE-10"
    ]
  },
  "notes": {
    "market_data": "Correlation, fragility and cross-instrument stress need a common window across TWO OR MORE instruments. Report the symbol count, never 'the table is empty' -- that phrasing goes stale on the first ingestion and did.",
    "authority": "A measurement of the deployment, not a claim that anything is correct. The RiskEngine remains the final veto."
  }
}
