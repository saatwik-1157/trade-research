-r requirements.txt
pytest>=8
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.6
mypy>=1.11
aiosqlite>=0.20   # in-memory database for auth tests
# L41. `tests/test_deployment.py` parses the compose files to assert that
# production publishes no datastore port and that the API and the worker
# split the background work. Declared because CI installs ONLY this file:
# it was available locally as a transitive dependency and would have failed
# collection on a clean runner. Found by the L42 dependency audit.
PyYAML>=6
# L82. `tests/test_monitoring.py` locks the backend's Benjamini-Hochberg to the
# original in `tools/patterns.py`, and importing that module pulls pandas.
# Declared for the same reason as PyYAML above: CI installs ONLY this file, and
# pandas was present locally because the research toolkit's own requirements
# are installed on a development machine. The first CI run that ever reached
# `pytest` failed on it.
pandas>=2.0

