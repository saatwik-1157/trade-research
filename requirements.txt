# Python 3.10 or newer: the tools use PEP 604 unions (str | None).
yfinance>=1.6.0
pandas>=2.0
numpy>=1.24
requests>=2.28

# Optional, Windows-only. Needed only by tools/mt5_account.py and
# tools/mt5_paper.py; every other tool runs without it, so it is left out of
# the install above rather than breaking Linux and macOS setups.
#   pip install MetaTrader5
